name: Apply Phase 7 plan review clarifications

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/apply-phase7-plan-review.py
      - .github/workflows/phase7-plan-review-apply.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1.3-phase7-release
          fetch-depth: 0
      - run: python scripts/apply-phase7-plan-review.py
      - run: |
          rm -f .github/workflows/phase7-plan-review-apply.yml
          rm -f scripts/apply-phase7-plan-review.py
          touch docs/.phase7-plan-review-ready
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: clarify Phase 7 evaluation and package paths [phase7-plan-review-applied]"
          git push origin HEAD:agent/v1.3-phase7-release
