name: Apply resumable-run integration patch

on:
  push:
    branches:
      - agent/v1.4-5-resumable-runs
    paths:
      - scripts/apply-v1-4-5-integration.py
      - .github/workflows/apply-v1-4-5-integration.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1.4-5-resumable-runs
          fetch-depth: 0
      - run: python scripts/apply-v1-4-5-integration.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src tests
          git commit -m "feat: integrate resumable-run controls" || exit 0
          git push origin HEAD:agent/v1.4-5-resumable-runs