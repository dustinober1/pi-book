name: Apply Phase 6 runtime fixes

on:
  push:
    branches:
      - agent/v1.3-phase6-research-wizard
    paths:
      - scripts/apply-phase6-runtime-fixes.py
      - .github/workflows/phase6-runtime-fixes-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1.3-phase6-research-wizard
          fetch-depth: 0
      - run: python scripts/apply-phase6-runtime-fixes.py
      - run: |
          rm -f .github/workflows/phase6-runtime-fixes-apply.yml
          rm -f scripts/apply-phase6-runtime-fixes.py
          touch docs/.phase6-runtime-ready
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: preserve wizard runtime compatibility [phase6-runtime-applied]"
          git push origin HEAD:agent/v1.3-phase6-research-wizard
