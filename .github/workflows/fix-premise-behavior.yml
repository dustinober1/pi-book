name: Fix premise behavioral integration

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/fix-premise-behavior.py
      - .github/workflows/fix-premise-behavior.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1.4-4-premise-lab
          fetch-depth: 0
      - run: python scripts/fix-premise-behavior.py
      - run: |
          rm -f .github/workflows/fix-premise-behavior.yml
          rm -f scripts/fix-premise-behavior.py
          touch docs/.premise-behavior-ready
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: align premise prompts and wizard copy [premise-behavior-fixed]"
          git push origin HEAD:agent/v1.4-4-premise-lab
