name: Apply premise task 1

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/apply-premise-task1.py
      - .github/workflows/premise-task1-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v1.4-4-premise-lab
          fetch-depth: 0
      - run: python scripts/apply-premise-task1.py
      - run: |
          rm -f .github/workflows/premise-task1-apply.yml
          rm -f scripts/apply-premise-task1.py
          touch docs/.premise-task1-ready
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: register premise lab schema [premise-task1-applied]"
          git push origin HEAD:agent/v1.4-4-premise-lab
