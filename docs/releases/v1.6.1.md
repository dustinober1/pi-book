# Novel Forge v1.6.1 — Pi Extension Compatibility

v1.6.1 fixes an extension-startup compatibility issue in Pi 0.80.10 and compatible runtimes.

## Fixed

- Thriller evidence IDs now use TypeBox's portable string-pattern schema instead of the unavailable `Type.RegExp` constructor.

## Upgrade

Install from the pinned `v1.6.1` tag. This patch changes no manuscript text, project schema, workflow state, or evidence content.
